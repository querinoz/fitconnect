{
  "protocol": "FitConnect v1 — Elite Core / native Android rewrite",
  "phase": "F1",
  "phaseGate": "Mesmo FIT → mesmo output em JNI, WASM e napi. ≥15 golden files, desvio <1% vs Golden Cheetah.",
  "status": "IN_PROGRESS",
  "pauseReason": null,
  "startedAt": "2026-08-07T00:00:00Z",
  "lastUpdated": "2026-08-07T17:00:00Z",
  "decisionsApplied": {
    "D1": "ADR-005 accepted. Engineering writes Kotlin/Rust, owner reviews. Kill switch documented in ADR-005, checked at F2 gate.",
    "D2": "ADR-009 written. Supabase Postgres for v1, TimescaleDB/Neon deferred with explicit migration triggers.",
    "D3": "OPEN — hardware not specified. Blocks F4/F5 gates only. See HUMAN-QUEUE.md.",
    "D4": "OPEN — legal review status unknown. F8 scoped without archive-ZIP import (F8b when cleared).",
    "D5": "Done — empty compiling wear/ module exists. Build decision revisited at F13 gate.",
    "D6": "Claude Code + Cowork chosen by owner; this session ran in Cursor with substitutes (web search for docs, gradle/cargo/adb CLI). MCP 'verify with eyes' gap still open in HUMAN-QUEUE."
  },
  "gates": {
    "F0": "CLOSED 2026-08-07 — see qa/reports/gate-F0.md. CI-on-GitHub confirmation pending first push."
  },
  "completedSteps": [
    "F0: ADRs 005-009, BACKLOG-V2 contract",
    "F0: toolchains (rustup, Android SDK/NDK, JDK17) installed and env vars persisted",
    "F0: elite-core Rust workspace (core/jni/wasm/napi) — test/clippy/fmt/wasm32 all clean",
    "F0: android/ Gradle project — :app :wear :core-capture :design, wrapper 9.5.0, AGP9 built-in Kotlin, full build green",
    "F0: token pipeline — tokens:kotlin generate + check (CI drift gate), EliteSurfaceTokens.kt compiles",
    "F0: packages/elite-core-wasm wrapper (tests pass without artifact)",
    "F0: apps/mobile frozen (README banner, out of CI graph)",
    "F0: CI workflows elite-core-rust.yml + android.yml",
    "F0: internal skills elite-surface / sports-metrics / elite-core-rust",
    "F1: streams module (time-series model, validation invariants, README, 6 tests)",
    "F1: metrics slice 1 — NP/IF/TSS + power curve per sports-metrics.md §1.1-1.4 (README, 15 tests) — note: power curve was already implemented as part of this slice, not a separate later step; STATE.json previously mis-listed it as pending",
    "F1: zones module — power 7-zone (§4) + HR 5-zone (§2.2) ladder, time-in-zone distribution (README, 9 tests)",
    "F1: metrics slice 2 — hrTSS per sports-metrics.md §2.1, TRIMP-based, Sex-coefficient enum, LTHR-calibrated (README, 8 tests)",
    "F1: physiology module — CTL/ATL/TSB (§5.1), ACWR (§5.2 + advisory band), rMSSD baseline + readiness flag (§5.3) (README, 14 tests)",
    "F1: metrics slice 3 — pace/GAP (§3.1, Minetti cost-of-running polynomial) + NGP + IF_pace + rTSS (§3.2) (README, 17 tests)"
  ],
  "pendingSteps": [
    "F1: fit module — FIT parser (records → ActivityStreams), serde-based, fuzz boundary",
    "F1: FTP estimation (§1.5) + LTHR/threshold-pace estimation (§2.2/§3.2) — best-effort-from-power/HR/pace-curve heuristics, app-layer callers of the primitives already in core",
    "F1: laps (§6) + GPS filtering (§7)",
    "F1: golden files — need ≥15 real FIT files + Golden Cheetah exports (HUMAN-QUEUE if owner has none)",
    "F1: real bindings — UniFFI Kotlin AAR, wasm-pack artifact into packages/elite-core-wasm, napi module",
    "F1: cross-target parity test in CI (same FIT → identical JSON on JNI/WASM/napi)",
    "verify CI green on GitHub after push (F0 conditional item)"
  ],
  "needsHuman": [
    "D3 — hardware (phone, watch, 3 HR straps, power meter, reference watch) — before F4",
    "D4 — legal review status for archive import (F8b)",
    "MCP tooling for visual verification (Playwright + Android emulator/Maestro) — before F3",
    "Real FIT files from owner's devices for the F1 golden set (public samples used otherwise)",
    "Veto window on applicationId com.fitconnect.android (closes at first Play upload, F15)"
  ]
}
