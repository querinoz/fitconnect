# Copy to android/keystore.properties (gitignored) for signed release/AAB.
# NEVER commit real passwords or the .jks/.keystore file.

storeFile=release.keystore
storePassword=REPLACE_ME
keyAlias=fitconnect
keyPassword=REPLACE_ME
