package com.fitconnect.android.auth

/**
 * Synthetic Firebase account for Android instrumentation (fitconnect-5d2ba).
 * Provision before tests: pnpm p1-auth:provision-android-user
 */
object P1AuthTestAccount {
    const val EMAIL = "p1auth.instrumentation@fitconnect-qa.invalid"
    const val PASSWORD = "P1AuthInstr!2026"
}
