package com.fitconnect.android

import org.junit.Assert.assertTrue
import org.junit.Test

/** F0 placeholder — proves the JVM unit test source set and JUnit are wired up. */
class SkeletonTest {
    @Test
    fun placeholder() {
        assertTrue(true)
    }
}
