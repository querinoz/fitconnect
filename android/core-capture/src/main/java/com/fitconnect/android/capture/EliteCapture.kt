package com.fitconnect.android.capture

/**
 * Elite Capture facade — outdoor GPS is wired through [runtime.OutdoorCaptureRuntime].
 * No production simulation path.
 */
object EliteCapture {
    const val MODULE: String = "core-capture"
    const val SOURCE_GPS: String = "SOURCE_GPS"
}
