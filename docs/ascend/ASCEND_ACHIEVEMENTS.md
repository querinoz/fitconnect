# ASCEND Achievements™

Registry: `AchievementRegistry.ALL` — curated, not hundreds of junk badges.

Rarity: COMMON → MYTHIC.

Population line **“owned by X%” is forbidden** without real population data. LOCAL_DEMO uses `ach.owned.demo`: simulated rarity, not population data.

XP on unlock is added once during fold (idempotent with the event log).

Community `:community` `AchievementEngine` remains for social awards. ASCEND is the athlete progression source of truth.
