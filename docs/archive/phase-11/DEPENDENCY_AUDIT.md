# Dependency Audit

No Expo/RN imports in android/.
NoOp analytics/image/realtime/push — optional until needed.
OkHttp required. EncryptedSharedPreferences required for security.
:community not on :app classpath (ok).
