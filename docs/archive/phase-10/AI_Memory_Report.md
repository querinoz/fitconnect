# AI Memory Report

Tiers: SESSION, SHORT_TERM, LONG_TERM_PREF, COACH_CONTEXT, PROGRAM_CONTEXT.

Conversations are not auto-persisted. Long-term preferences require explicit `rememberPreference`.
`forget` removes user-controlled entries. Session buffers are clearable.
