# AI Grounding Report

Every insight cites `Evidence` with `sourceEngine`.
Recommendations require insight evidence.
Missing data → `Confidence.INSUFFICIENT_DATA` + explicit unavailable messaging.
Local provider refuses to invent when context lacks AVAILABLE/EVIDENCE lines.

Target grounding ≥95%: golden + unit suite all grounded (0 fabrication failures).
