# AI Android QA

Validated by compile + unit tests + debug assemble:

- Athlete AI screen + deep link
- Coach AI screen + deep link
- AiInsightCard a11y contentDescription
- Dark Elite OS tokens via design-ui
- Maestro script: `maestro/android/smoke-ai-performance.yaml` (device run pending)

Device matrix (13/14/15, foldable, landscape): pending physical/device farm — not blocking architecture gates; smoke script ready.
