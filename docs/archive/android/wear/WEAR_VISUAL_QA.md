# Visual QA

Elite OS tokens: floor `#070B14` (`EliteSurfaceColors.FLOOR`), Volt `#C8FF00`, Connect `#00DDB4`, Syne / Plus Jakarta / JetBrains via design-ui.

Wear uses the same `EliteSurfaceColors` — not default Material teal.

Compare screenshots only when captured from a running emulator/device:

AUTH, ATHLETE home, COACH overview, READINESS, WORKOUT, MAP (`elite_route_map`), SUMMARY share card, SLEEP, RECOVERY.

Without captures: **VISUAL = UNVERIFIED**. Do not treat code-token usage as a visual PASS.
