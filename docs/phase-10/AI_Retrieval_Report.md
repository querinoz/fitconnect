# AI Retrieval Report

`RetrievalEngine` stores `KnowledgeDoc` with provenance:
source kind, id, section, version, owner, permissions, timestamp.

Search is keyword-scored with kind filters. Health time-series are never embedded.
Community content is not treated as system knowledge.
