# Sports Network (V11)

## Models
- Spots (public approx coords only)
- Secret spots (owner/shared; exact coords never public)
- Events + join
- Challenges (domain)

## API
`GET/POST /api/v1/network`

## Policy
Strava activities never enter social graph (existing community-social-policy).
