# Phase 06 — Sports Registry

`SportsRegistry` / `DefaultSportsRegistry`:

- `register(definition, pluginId?)`
- `deprecate(id)`
- `get` / `require` / `all` / `byCategory` / `discover`
- `capabilities` / `dependencies` / `version`
- `validate` / `snapshotJson`

Seed: `DefaultSportsCatalog` (13+ sports including yoga, rowing). Future sports via `register(..., pluginId)`.
