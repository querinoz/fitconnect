# Phase 07 — Availability Engine

Working hours · breaks · vacations · recurring rules · blocked dates · time zones · private/group capacity.

`isOpen(targetId, start, duration, mode)` gates booking creation.
