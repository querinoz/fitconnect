# Phase 05 — Technical Debt

1. **Pointer drag-and-drop calendar** — reschedule via +1h actions; gesture DnD next.  
2. **Stripe Connect live SDK** — architecture gateway only.  
3. **AI Engine** — `CoachAiPort` empty adapters by design.  
4. **Room durable coach cache** — in-memory local adapter.  
5. **Voice note capture UI** — inbox kind supported; recorder not wired.  
6. **Paged roster for 10k+ athletes** — filter API ready; paging adapter pending.  
7. **Device Maestro / TalkBack / Visual QA** — emulator blocked.
