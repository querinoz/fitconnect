# Strava 2026 compliance checklist

Source: FitConnect architecture (`AGENTS.md` §1–§5).

## 5. Pre-ship

| # | Item | Result |
|---|---|---|
| 1 | No STRAVA session visible to a third party (DB `shareable` + RLS) | PASS — `supabase/migrations/011_workout_sessions_shareable.sql` + `workout-session-policy.test.ts` |
| 2 | Same barrier on Android social queries | PASS — `SocialSessionQueries` + `StravaSocialBarrierTest` |
| 3 | Badges: `privateProgress` vs `shareableProgress` | PASS — `BadgeProgressEngine` |
| 4 | No `client_secret` in APK | PASS — OAuth URL is edge-only (`StravaAuth.authorizeUrl`) |
| 5 | Rate limit brake at 85% | PASS — `isRateLimited(..., 0.85)` + `StravaRateLimitInterceptor` |
| 6 | Webhook handshake `<2s` | PASS — GET returns challenge JSON synchronously |
| 7 | `athlete.update` `authorized=false` purges tokens **and** derived rows | PASS — `isStravaAthleteRevocation` + `markDeauthorized` deletes activities then connection |
| 8 | Banned endpoints not called | PASS — `StravaPathAllowlist` |
| 9 | Powered by Strava / Connect with Strava on Strava surfaces | PARTIAL — copy helpers exist; official brand assets not bundled |
| 10 | No Google Fit | PASS — grep `com.google.android.gms.fitness` empty |

Do not treat a UI-only hide as compliance.
