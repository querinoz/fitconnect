# Battery Report

No persistent GPS/WorkManager/realtime sockets active (NoOp ports).
BackgroundSyncPolicy remains the contract for future WorkManager wiring.
DEBUG dual sync no longer runs every cold start.
Location: still mock/contract-only — when live, require displacement + interval ≥30–60s.
