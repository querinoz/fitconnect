{
  "name": "@fitconnect/api-client",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run --config vitest.config.ts --exclude src/pacts/**",
    "test:pact": "vitest run --config vitest.config.ts src/pacts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fitconnect/types": "workspace:*",
    "@trpc/server": "^11.1.0",
    "superjson": "^2.2.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.2.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
