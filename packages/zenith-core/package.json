{
  "name": "@fitconnect/zenith-core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic Zenith Performance Engine — scores, baselines, trends. LLM-free.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fitconnect/types": "workspace:*",
    "@fitconnect/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
