# DISABLED — Expo `apps/mobile` removed from the tree (ADR-005, 2026-09-08).
# Production mobile is native Android Compose (`android/`). iOS is `iosApp/` (SwiftUI).
name: EAS Preview (disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    name: Expo removed — use android/
    runs-on: ubuntu-latest
    steps:
      - name: Fail with archive notice
        run: |
          echo "Expo apps/mobile was removed (ADR-005). Recover from git history if needed."
          echo "Build native APK: cd android && ./gradlew :app:assembleDebug"
          exit 1
