{
  "name": "fitconnect-firebase-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "backfill-role-claims": "node ../scripts/p1-auth-firebase-role-backfill.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^7.2.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
