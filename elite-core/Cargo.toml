[workspace]
resolver = "2"
members = ["core", "jni", "wasm", "napi"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "UNLICENSED"
publish = false

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
