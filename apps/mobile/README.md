# @fitconnect/mobile

> ⚠️ **FROZEN — LEGACY (2026-08-07).** This Expo app is superseded by the native Android app in `android/` (see [docs/adr/ADR-005-expo-to-native-android.md](../../docs/adr/ADR-005-expo-to-native-android.md)). No new features. Reusable here: screen inventory, i18n key shapes, readiness copy. Will be archived after F6 proves the native shell.

Legacy Expo 52 / expo-router app. Excluded from required CI (typecheck + unit tests) so a frozen app cannot break the pipeline — see `.github/workflows/ci.yml`.

To run it locally anyway:

```bash
pnpm dev:mobile
```
